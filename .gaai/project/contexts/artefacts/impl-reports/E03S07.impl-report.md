---
type: artefact
artefact_type: impl-report
id: E03S07
story_id: E03S07
created_at: 2026-03-16
status: complete
---

# Implementation Report — E03S07: N8N On Site Customer SMS + Status Update

## Summary

Delivered an n8n polling workflow (`E03S07-on-site-customer-sms.json`) and supporting Supabase schema migration (`E03S07-add-on-site-sms-sent.sql`) that:

- Polls every 30 seconds for jobs with `status = 'On Site'` AND `on_site_sms_sent = false`
- For each match: fetches `business_name` from `clients`, builds canonical SMS, sends via native Twilio node
- Marks `on_site_sms_sent = true` after send (or on null-phone skip) — prevents duplicate sends
- Handles `customer_phone = null` gracefully with no error (AC5)
- SMS failure does not revert job status — `continueOnFail: true` on Twilio node (AC6)
- Twilio `From` field uses `FROM_NUMBER_PLACEHOLDER` — human replaces when campaign approved
- Split In Items rewiring issue documented prominently in pre-activation checklist

## Files Changed

| File | Change |
|------|--------|
| `n8n-workflows/E03S07-on-site-customer-sms.json` | Created — n8n polling workflow (8 nodes, mirrored from E03S06) |
| `supabase/migrations/E03S07-add-on-site-sms-sent.sql` | Created — additive `ALTER TABLE` adding `on_site_sms_sent boolean DEFAULT false` |

## Acceptance Criteria Mapping

| AC | Implementation | Location |
|----|---------------|----------|
| AC1 | Schedule Trigger (30s) polls `status=eq.On%20Site&on_site_sms_sent=eq.false`; PATCH writes only `on_site_sms_sent=true` — `status` field never touched | `GET Jobs Pending On Site SMS` + `PATCH jobs — Mark On Site SMS Sent` |
| AC2 | Code node assembles `businessName + ': Your technician has arrived.'` — exact canonical template | `Build SMS Body` node |
| AC3 | `business_name` fetched from `clients?id=eq.[client_id]` — never hardcoded | `GET Client Details` + `Build SMS Body` |
| AC4 | `n8n-nodes-base.twilio` typeVersion 1 — native Twilio node, not HTTP Request | `Send Customer SMS` node |
| AC5 | IF node checks `customer_phone` non-empty; false branch routes directly to PATCH — no Twilio call, no error | `Customer Phone Present?` node, false branch |
| AC6 | `continueOnFail: true` on Twilio node; PATCH writes only `on_site_sms_sent`, never `status` | `Send Customer SMS` node + `PATCH jobs` node |

## Rules Applied

- DEC-003: HTTP Request nodes for all Supabase calls (GET jobs, GET clients, PATCH jobs)
- DEC-006: Native n8n Twilio node for SMS — NOT HTTP Request
- conventions.md Twilio SMS: native Twilio node, `From = clients.twilio_number` (stored; actual campaign number = placeholder)
- n8n 2.x node reference pattern: `$('Node Name').item.json.field` throughout
- Code node output: `return [{ json: { ... } }]` pattern
- SUPABASE_ANON_KEY_PLACEHOLDER convention: all HTTP Request nodes use placeholder
- `FROM_NUMBER_PLACEHOLDER` used for Twilio From field (campaign not yet approved per user context)

## Workflow Node Map

| # | Node | Type | Purpose |
|---|------|------|---------|
| 1 | Poll: Every 30 Seconds | scheduleTrigger 1.2 | Fires every 30s |
| 2 | GET Jobs Pending On Site SMS | httpRequest 4.2 | Poll jobs table for On Site + unsent |
| 3 | Split In Items | splitInBatches 3 | Process each job independently |
| 4 | GET Client Details | httpRequest 4.2 | Fetch business_name + twilio_number |
| 5 | Customer Phone Present? | if 2 | AC5 guard — null phone skip |
| 6 | Build SMS Body | code 2 | Assemble canonical On Site template |
| 7 | Send Customer SMS | twilio 1 | Native Twilio send (continueOnFail) |
| 8 | PATCH jobs — Mark On Site SMS Sent | httpRequest 4.2 | Set on_site_sms_sent=true |

## Difference From E03S06 (Three Changes Only)

| Element | E03S06 | E03S07 |
|---------|--------|--------|
| Status filter | `En Route` | `On Site` |
| Sentinel column | `en_route_sms_sent` | `on_site_sms_sent` |
| SMS template | `Your technician is on the way.` | `Your technician has arrived.` |

All other structure, node types, connections, and patterns are identical.

## Known Risks / Limitations

- **Twilio From placeholder:** `FROM_NUMBER_PLACEHOLDER` must be replaced with the approved 10DLC number before the workflow is activated. Explicit known condition per user instruction — not a delivery gap.
- **Split In Items rewiring:** Must manually rewire to `loop` output after n8n JSON import (known v2.6.4 bug). Prominently documented in node notes and `_comment` pre-activation checklist.
- **No new RLS policy needed:** `jobs` table anon UPDATE policy already confirmed by E03S05 + E03S06. Documented in migration file.
