---
type: artefact
artefact_type: qa-report
id: E03S07
story_id: E03S07
created_at: 2026-03-16
verdict: PASS
---

# QA Report — E03S07: N8N On Site Customer SMS + Status Update

## Verdict: PASS

**6/6 acceptance criteria met. No rule violations. No remediation required.**

---

## Per-Criterion Results

| AC | Result | Evidence |
|----|--------|----------|
| AC1 | PASS | Schedule Trigger fires every 30s (< 60s SLA). Poll filter: `status=eq.On%20Site&on_site_sms_sent=eq.false`. PATCH body writes only `on_site_sms_sent=true` — `status` field never touched anywhere in the workflow. |
| AC2 | PASS | `Build SMS Body` code node: `businessName + ': Your technician has arrived.'` — exact canonical template match. |
| AC3 | PASS | `GET Client Details` fetches `clients?id=eq.[client_id]&select=business_name,twilio_number`. Code node reads `$('GET Client Details').item.json.business_name`. No hardcoded names anywhere. Throws if `business_name` missing. |
| AC4 | PASS | `Send Customer SMS` node: `"type": "n8n-nodes-base.twilio"`, `"typeVersion": 1`. Native Twilio node — not HTTP Request. Compliant with conventions.md and DEC-006. |
| AC5 | PASS | `Customer Phone Present?` IF node routes null/empty `customer_phone` directly to PATCH, bypassing `Build SMS Body` and `Send Customer SMS`. No error path triggered on null phone. |
| AC6 | PASS | `Send Customer SMS` has `continueOnFail: true`. PATCH body is `{"on_site_sms_sent": true}` only — `status` field never written. SMS delivery failure cannot affect job status. |

---

## Convention Compliance

| Convention | Status |
|-----------|--------|
| Supabase HTTP Request (DEC-003) | PASS — all 3 Supabase nodes use httpRequest typeVersion 4.2, not native Supabase node |
| Native Twilio node (DEC-006) | PASS — `n8n-nodes-base.twilio` typeVersion 1 |
| n8n 2.x expression pattern | PASS — `$('Node Name').item.json.field` used throughout; `$json.field` not used for upstream data |
| Code node output format | PASS — `return [{ json: { ... } }]` in Build SMS Body |
| SUPABASE_ANON_KEY_PLACEHOLDER | PASS — placeholder in all 3 HTTP Request nodes (GET jobs, GET client, PATCH) |
| No hardcoded business names | PASS — business_name always from clients table |
| Twilio From placeholder | PASS — `FROM_NUMBER_PLACEHOLDER` documented with clear replacement instructions; campaign not yet approved per user context |

---

## Schema Change Review

Migration: `supabase/migrations/E03S07-add-on-site-sms-sent.sql`
- `ALTER TABLE jobs ADD COLUMN IF NOT EXISTS on_site_sms_sent boolean DEFAULT false`
- Additive — no breaking changes to existing columns or rows
- `IF NOT EXISTS` guard prevents failure on re-run
- Default `false` is correct — all existing rows treated as not-yet-sent
- No new RLS policy required — anon UPDATE policy already confirmed by E03S05 + E03S06

---

## Known Issues Verification

| Known Issue | Addressed? | Evidence |
|-------------|-----------|---------|
| Split In Items rewired to done output after import | YES — documented | Node notes + `_comment` pre-activation checklist item 5: explicit warning and fix instruction |
| Twilio FROM_NUMBER_PLACEHOLDER | YES — documented | Schedule trigger note + Twilio node notes + pre-activation checklist item 3 |
| No new anon UPDATE policy needed | YES — confirmed | Migration file comment explicitly states policy already exists |

---

## Remediation Attempts

None. Clean PASS on first review.
