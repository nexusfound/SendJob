---
type: artefact
artefact_type: qa-report
id: E02S08
story: contexts/artefacts/stories/E02S08.story.md
plan: contexts/artefacts/plans/E02S08.execution-plan.md
impl-report: contexts/artefacts/impl-reports/E02S08.impl-report.md
created_at: 2026-03-15
verdict: PASS
attempt: 1
---

# QA Report — E02S08: Dispatcher Login (Supabase Auth)

## Verdict: PASS

All 8 acceptance criteria satisfied. No rule violations. No regressions.

---

## Acceptance Criteria Validation

| AC | Criterion | Result | Evidence |
|----|-----------|--------|----------|
| AC1 | Unauthenticated → login form only | PASS | `#app-view` hidden on init; `getSession()` → `showLoginView()` when no session |
| AC2 | Form collects email/password, calls `signInWithPassword` | PASS | `supabaseClient.auth.signInWithPassword({email, password})` in submit handler |
| AC3 | Successful login shows job queue, hides login form | PASS | `showAppView()` hides `#login-view`, shows `#app-view`, calls `loadJobs()` |
| AC4 | Failed login shows "Invalid email or password." without clearing email | PASS | Error set on `result.error`; email input not touched |
| AC5 | Session persists on page refresh | PASS | `getSession()` on DOMContentLoaded restores session from `localStorage` (Supabase v2 default) |
| AC6 | Visible logout, returns to login on click | PASS | `#logout-btn` in nav, `signOut()` → `showLoginView()` |
| AC7 | Mobile 375px — 16px font, 52px tap targets | PASS | `.login-input` and `.login-submit`: `font-size: 16px`, `min-height: var(--tap-min)` (52px) |
| AC8 | RLS: authenticated reads/writes pass | PASS | All fetches use `Bearer ${currentAccessToken \|\| SUPABASE_ANON_KEY}` — user JWT sent when logged in |

---

## Scope Integrity

- Only `dispatcher-dashboard/index.html` modified — within scope
- No password reset, social login, or multi-dispatcher features added
- No unrelated refactoring
- PASS

---

## Rule Enforcement

- Design system: `--navy-mid` card, `--border` borders, `--ice` focus ring, Barlow fonts, `--tap-min` tokens — all per Playbook design system
- Error handling: all auth calls and fetch calls have error handlers
- Single responsibility: `showAppView`, `showLoginView`, submit handler, logout handler each do one thing
- Identifiers explicit: `currentAccessToken`, `loginError`, `showAppView`, `showLoginView`
- PASS

---

## Regression Scan

- `loadJobs()`, `loadTechs()`, `assignTech()` logic unchanged — Authorization header improvement is additive
- `statusDot`/`statusLabel` references intact; `showLoading()` still fires on `loadJobs()` call
- No tests existed prior (static HTML project) — no test regressions possible
- PASS

---

## Quality Checks

- Error-prone operations (auth, fetch): all have `.catch()` or `.then(result.error)` handlers
- External inputs (`emailValue`, `passwordValue`): passed directly to Supabase SDK which validates server-side; `escapeHtml` applied in existing code for DOM rendering
- No dead code or unreachable branches
- PASS

---

## Notes

- `getSession()` result may take <50ms to resolve from `localStorage`; the status-dot briefly shows "Loading…" during this window — cosmetically acceptable
- RLS note in code and impl-report clarifies dispatcher account must be created in Supabase Auth before live testing (out of scope for this story's code changes)
