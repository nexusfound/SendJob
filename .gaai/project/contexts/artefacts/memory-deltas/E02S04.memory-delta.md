---
type: artefact
artefact_type: memory-delta
id: E02S04
story: contexts/artefacts/stories/E02S04.story.md
qa-report: contexts/artefacts/qa-reports/E02S04.qa-report.md
created_at: 2026-03-17
durable_decisions: false
---

# Memory Delta — E02S04: Already-Assigned Guard SMS

## Durable Decisions

No new architectural or governance decisions emerged from this delivery. The implementation
followed established patterns exactly — no deviations, no novel decisions.

## Pattern Confirmations

The following existing patterns were confirmed in practice:

1. **Supabase foreign key join in n8n**: `?select=id,status,tech_id,techs(name)` correctly returns
   `job.techs = { name: "..." }` or `null` — no additional nodes needed for joined data.
   This is the same pattern used in browser JS (`?select=*,techs(name)`) confirmed working.

2. **IF node false branch wiring (n8n v2 import bug)**: Confirmed again — both `Tech Found?` and
   `Job is New?` false branches require manual re-wiring after JSON import. This is a recurring
   operational step, not a bug in the workflow JSON. Already documented in feedback files.

3. **Guard SMS pattern**: False-branch guard SMS (inform dispatcher without modifying data) is a
   reusable pattern: GET current state → Code node to build message → native Twilio node.
   Used identically in E02S03 (tech not found) and E02S04 (job not New).

## Conventions Unchanged

No updates needed to `conventions.md` or `decisions/_log.md`. All patterns were pre-existing.
